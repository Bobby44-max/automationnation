# Rain Check Agent Server Environment
# Copy to .env and fill in values

# Shared secret between Vercel and VPS (generate with: openssl rand -hex 32)
AGENT_SERVER_SECRET=

# Anthropic API key for Claude CLI
ANTHROPIC_API_KEY=

# Port (default: 3848 — EduPrep agent uses 3847)
PORT=3848
